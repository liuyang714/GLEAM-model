# ============================================================
#  MEST-C Component Classification Config
# ============================================================

common:
  seed: 1208
  cuda_device: "0"

data:
  partition_csv: "/path/to/partition.csv"
  image_feat_dir: "/path/to/conch_features"

mestc:
  save_dir: "/path/to/save_dir"
  batch_size: 1
  lr: 1.0e-4
  epochs: 100
  num_workers: 0
